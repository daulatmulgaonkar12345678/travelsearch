#!/bin/bash
# Generate architecture diagram from Mermaid
# Requires mmdc (mermaid-cli) or use online tools

echo "Architecture diagram saved in ARCHITECTURE.md"
echo "To generate PNG, use: https://mermaid.live or install mermaid-cli"
echo "  npm install -g @mermaid-js/mermaid-cli"
echo "  mmdc -i ARCHITECTURE.md -o architecture.png"
