# API Configuration
# For local development, leave empty to use Next.js rewrites
# For production, set to the actual backend URL
NEXT_PUBLIC_API_BASE=

# Backend URL for server-side API routes (proxying)
# This is used by Next.js API routes to proxy requests to FastAPI
BACKEND_URL=http://localhost:8001
